import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  buttonStyleBase: {
    backgroundColor: "#ccc",
    paddingVertical: 12,
    paddingHorizontal: 25,
    borderRadius: 25,
  },
  buttonTextStyleBase: {
    color: "#fff",
    fontSize: 18,
    textAlign: "center"
  },
});