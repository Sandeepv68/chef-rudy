import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  placeHolderContainer: {
    display: "flex",
    flexWrap: "wrap",
    flexDirection: "row",
    marginRight: 20,
    marginLeft: 20,
  },
});
