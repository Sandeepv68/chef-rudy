import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  placeHolderMedia: { width: 185, height: 160, marginBottom: 10, margin: 10 },
  placeHolderContainer: {
    display: "flex",
    flexWrap: "wrap",
    flexDirection: "row",
    marginRight: 20,
    marginLeft: 20,
  },
});
